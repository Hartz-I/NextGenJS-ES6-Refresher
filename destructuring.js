//assigning all the properties in an object to different variables

//of arrays

const Dnumbers = [1, 2, 4];

[x, y, z] = Dnumbers;

console.log(y);

//object

const myObj = {
  name: "doc",
  age: 24,
};

const { name } = myObj;

console.log(name);
